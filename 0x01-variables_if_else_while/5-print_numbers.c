#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0(Success)
 */

int main()
{
	char a = '0';
	putchar(a);
	putchar(++a);
	putchar(++a);
	putchar(++a);
	putchar(++a);
	putchar(++a);
	putchar(++a);
	putchar(++a);
	putchar(++a);
	putchar(++a);
	putchar('\n');
	return 0;
}
