#include "main.h"

/**
 * _abs - Absolute value of a number
 * @n: number to be absoluted...yes that's a word
 *
 * Return: Absolute value of the number
 */

int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	return (n * -1);
}
